# The CLI loads this file via --env-file .env. Existing environment wins.
# Never commit the real .env file. Model/embedding selection stays in DeepTutor.
GALLOP_VAULT_PATH=
GALLOP_DEEPTUTOR_PATH=
GALLOP_STATE_PATH=
GALLOP_LOG_PATH=
GALLOP_DEEPTUTOR_HOME=
