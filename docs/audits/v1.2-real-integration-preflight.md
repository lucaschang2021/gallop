# v1.2 Real Tutor Integration Preflight

Date: 2026-09-14

Status: **MCP TRANSPORT PASS; REAL DOGFOOD PASS**

## Runtime boundary

Four enabled local STDIO servers are registered for Mathematics, Statistics,
Finance, and CS/AI. Each server binds its subject at process launch and exposes
the same seven focused tools. The official Python MCP SDK completed native
Codex initialization at protocol `2025-06-18`; native `tools/list` returned the
subject-bound schemas and safety annotations.

The read-only runtime-status operation does not initialize the Journal. MCP
tests cover official handshake framing, subject rejection, fresh-process
restore, forced process termination, and exact duplicate checkpoint recovery.
No tool or transport code imports DeepTutor.

## Real storage preflight

The existing Obsidian Vault, its four subject roots, and their managed Home
regions exist. Existing projection destinations were backed up outside the
repository before integration. Registration and read-only discovery left their
hashes unchanged and did not create the learner Journal root.

The bound Gallop-Reader passed the current fail-closed identity check across:

- configured target and iCloud zone;
- provider object and parent identity;
- local and server metadata rows;
- Windows placeholder identity;
- pin and acknowledged sync state.

The legacy bridge Inbox was empty. No provider repair, migration, deletion, or
bidirectional synchronization was attempted.

## Evidence boundary

All automated MCP and four-subject fixtures use the isolated
`integration_tests` namespace. They are not learner evidence and were not
written to the real Vault or learner Journal. Real dogfood must be produced by
normal Lucas-to-Tutor interaction; Codex-generated content cannot substitute
for proof, simulation reasoning, closed-book derivation, No-Agent Coding, or
human authorship confirmation.

## Release follow-up

The desktop client must refresh its MCP inventory before a new real Tutor
session. The required real dogfood observations are recorded in the
[acceptance record](v1.2-real-dogfood-acceptance.md), and v1.2.0 Release Ready
is recorded by the stable baseline and CI run `35064712786`. This preflight does
not authorize new subjects, a Gallop UI, or any change to evidence authority.
