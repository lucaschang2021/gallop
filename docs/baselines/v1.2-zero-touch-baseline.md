# GALLOP v1.2 — ZERO-TOUCH LEARNING CONTINUITY BASELINE

Record date: 2026-09-14

Governed stable implementation:
`5c5ded1ecf163707fe818cbfea277abaa7b998b5`.

Baseline record commit: the commit containing this file. The record does not
self-reference an object ID because that would make the commit hash impossible
to stabilize.

Acceptance condition: the exact baseline commit completed the required GitHub
Actions matrix successfully. The stable release is tagged `v1.2.0`; no merge or
`main` modification was performed.

## Product boundary

- The four GPT Tutor conversations are the only learner-facing surface.
- Gallop remains headless and owns governance, append-only state, evidence,
  continuity, and projections.
- Obsidian and Gallop-Reader remain projections, never learner authority.
- DeepTutor has zero required runtime dependencies and is not on the v1.2
  critical path.
- There is no Gallop UI and no mandatory secondary AI.
- Competition Mathematics and Yau specialization are outside this baseline.

## Real acceptance

The dated [real dogfood record](../audits/v1.2-real-dogfood-acceptance.md)
establishes:

- four real subject-bound Tutor sessions;
- real Mathematics proof/mistake/repair and fresh-chat retest;
- real Statistics simulation reasoning;
- real Finance closed-book derivation;
- real CS/AI No-Agent Coding;
- incremental checkpoint, abrupt-close restore, exact duplicate recovery;
- automatic owned Obsidian projection, fail-closed backup-based projection
  recovery, and Reader publication recovery by validated republish;
- safe one-way PC to Vault to Gallop-Reader publication and learner-confirmed
  mobile visibility;
- four-subject isolation and conservative evidence authority.

The system acceptance is PASS even though the Mathematics independent retest
was `PARTIAL`: the required behavior was to preserve that result as `GUIDED`,
mastery level `0`, rather than lower the evidence standard for a release claim.
The baseline is frozen for v1.2.0. The Mathematics independent retest remains
`PARTIAL`, `GUIDED`, mastery level `0`; this is an evidence outcome, not a
release exception.

## Required engineering gates

The commit containing this record must pass all of the following locally and in
the configured Windows/Ubuntu, Python 3.11/3.13 CI matrix where applicable:

- full pytest regression;
- Ruff;
- scoped Mypy;
- architecture existing hard violations `0`, introduced hard violations `0`;
- repository privacy audit;
- protocol example validation and offline isolated demo;
- wheel build;
- V1 replay EXACT;
- mastery drift NONE;
- queue drift NONE;
- evidence drift NONE.

Local pre-freeze verification on Windows/Python 3.13 completed with 374 tests,
Ruff PASS, Mypy PASS across 20 source files, and architecture hard violations
`existing=0 / introduced=0`. Protocol examples and the isolated offline demo
passed. The stable package is `1.2.0`; the local wheel is
`gallop_learning-1.2.0-py3-none-any.whl`, SHA-256
`77f7ca40d335a755803d9087b2889e4aa7e6043a77995ec5af15140df4779b4a`.
The CI build gate must reproduce a successful wheel build, while byte-for-byte
wheel reproducibility is not claimed because archive timestamps are not frozen.

The frozen V1 replay source remains
`c8d0dfdcdee1dd002688ebf43d20bd2b48766fc8`: 38 events, state SHA-256
`9f70585b0454dcf00ddcbb14b616a34c285ede6394e913846719e959a382fba3`,
and projection SHA-256
`0fb261f68c2650e95b07642a33e70886e7302a8988f3973768108cbbe1e8d8f3`.

## Freeze rule

The exact baseline commit is green in [CI run 35064712786](https://github.com/lucaschang2021/gallop/actions/runs/35064712786).
Gallop v1.2.0 is ready for controlled daily use under the documented evidence,
privacy, Vault ownership, and one-way Reader boundaries. Do not automatically
enter Competition Mathematics, Yau specialization, or any new product scope.
