# Benchmark schema

The normative contract is
[`benchmark.schema.json`](../gallop/schemas/benchmark.schema.json). Score,
duration and counts are optional. Validation rejects impossible counts and
contradictory no-agent or closed-book contexts. Interpretation is documented in
the [Elite Training Protocol](elite-training-protocol.md).
